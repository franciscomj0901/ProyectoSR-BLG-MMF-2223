



# This file describes the network interfaces available on your system
# and how to activate them. For more information, see interfaces(5).

source /etc/network/interfaces.d/*

# The loopback network interface
auto lo
iface lo inet loopback

# The primary network interface
auto enp0s3
iface enp0s3 inet static
address 172.17.32.12
netmask 255.255.255.0

gateway 172.17.32.11

 # Se añade regla SNAT para paquetes que provenga de la red 1 y se reenvíen por la interfaz cal3, 
    # es dpaquetes que van dirigidos a Internet. 
    # con ‘-j MASQUERADE’ se indica que la IP-origen de los paquetes se modifique por la IP que 
    # tuviera la interfaz cal3 (esta interfaz está en la red NAT y está configurada por DHCP, por lo que 
    # puede cambiar, pero gracias a ‘-j MASQUERADE’ se obtiene la que tuviera en cada momento
 #   up iptables ROUTING -o  enp0s9  -s 172.17.31.0/24 -j MASQUERADE
  #  down iptables -t nat -D POSTROUTING -o enp0s9 -s 172.17.31.0/24 -j MASQUERADE
    # regla SNAT para paquetes que provengan de la red 2
   # up iptables -t nat -A POSTROUTING -o  enp0s9  -s 172.17.32.0/24 -j MASQUERADE
    #down iptables -t nat -D PO


#la otra
auto enp0s8
iface enp0s8 inet static
address 172.17.33.11
netmask 255.255.255.0
    
    
   


